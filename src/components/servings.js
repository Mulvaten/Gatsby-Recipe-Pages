import * as React from 'react'

const Servings = (servings) => {
    return (
        <div>
            <p>Number of servings:</p>
            <p>{ servings }</p>
        </div>
    )
}

export default Servings