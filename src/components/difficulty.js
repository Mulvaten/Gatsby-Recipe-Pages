import * as React from 'react'

const Difficulty = (difficulty) => {
    return (
        <div>
            <p>Difficulty:</p>
            <p>{ difficulty }</p>
        </div>
    )
}

export default Difficulty