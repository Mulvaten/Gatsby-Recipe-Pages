import * as React from 'react'

const Ingredients = (ingredients) => {
    return (
        <div>
            <h2>Ingredients:</h2>
            <p>{ ingredients }</p>
        </div>
    )
}

export default Ingredients